import LoginCard from "../components/LoginCard";

function login() {
  return (
    <main className="w-full h-screen items-center justify-center flex flex-col">
      <LoginCard />
    </main>
  );
}

export default login;
