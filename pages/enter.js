function enter() {
  const user = "";
  const username = {};
  function Signout() {
    return <button onClick={() => auth.signOut()}>SignOut</button>;
  }
  return (
    <div></div>
    // <main>{user ? !username ? <LoginCard /> : <Signout /> : <SignIn />}</main>
  );
}

export default enter;
